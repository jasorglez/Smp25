import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bi.red',
  appName: 'Red Ciudadana',
  webDir: 'dist/bi-aug-24/browser'
};

export default config;
