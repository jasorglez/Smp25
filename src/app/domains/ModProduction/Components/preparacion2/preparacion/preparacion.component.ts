import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({ selector: 'app-preparacion', standalone: true, imports: [CommonModule], template: `<p>Preparacion — en construcción</p>` })
export class PreparacionComponent {}
