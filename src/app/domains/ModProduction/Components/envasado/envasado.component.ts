import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-envasado',
  standalone: true,
  imports: [CommonModule],
  template: `<p>Envasado — en construcción</p>`,
})
export class EnvasadoComponent {}
