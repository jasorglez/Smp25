import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({ selector: 'app-licores', standalone: true, imports: [CommonModule], template: `<p>Licores — en construcción</p>` })
export class LicoresComponent {}
