import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-envasado-molienda',
  standalone: true,
  imports: [CommonModule],
  template: `<p>Envasado (Molienda) — en construcción</p>`,
})
export class EnvasadoMoliendaComponent {}
