services:
  Smp25:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # Lee ENVIRONMENT del .env (generado automáticamente por git hooks)
        ENVIRONMENT: ${ENVIRONMENT}
    image: smp25-frontend:latest
    container_name: Smp25-Frontend
    restart: unless-stopped
    ports:
      - "8080:80" # Puerto local:contenedor
    networks:
      - smp25-network
    labels:
      - "com.smp25.version=4.03"
      - "com.smp25.service=frontend"
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost/" ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

networks:
  smp25-network:
    driver: bridge
